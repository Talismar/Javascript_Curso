// Curso de Javascript #15

// primeira forma de criar um array
//var cor = new Array()

// segunda maneira
var cor = [1,2,3,4,5]
var cor2 = [6,7,8,9,10]


// Ordenar o array
// cor = cor.sort()

// Reverse array
// console.log(cor.reverse())

// Junta todos os elementos de um array em uma variavel em forma de string
// numeros = cor.join()
// console.log(numeros)

// Concar para conctenar dois array
// console.log(cor.concat(cor2))

// Tamanho de um array 
console.log(cor.length)

// Verificar se um elemento esta no array e a posição - if não estiver contido retorna -1
// console.log(cor.indexOf(11))

// if (cor.indexOf(11) == -1){
//   console.log("não tem")
// }
// else
//   console.log("Tem na posição - " + cor.indexOf(11))